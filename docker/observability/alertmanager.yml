global:
  resolve_timeout: 5m

route:
  receiver: 'ntfy'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h

receivers:
  - name: 'ntfy'
    webhook_configs:
      - url: 'http://ntfy:80/vetqueue-alerts?priority=5'
        send_resolved: true

